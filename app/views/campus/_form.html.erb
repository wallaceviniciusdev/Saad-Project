<%if current_user.type == "Administrator"%>
<%= form_for(@campu) do |f| %>
  <% if @campu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campu.errors.count, "error") %> prohibited this campu from being saved:</h2>

      <ul>
      <% @campu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="col-lg-12">

  <div class="row">
    <div class="form-group"> 
      <div class="col-lg-6">
        <%= f.label :name %><br>
        <%= f.text_field :name, class: "form-control" %>
      </div>
      <div class="col-lg-6">
        <%= label_tag :institution %><br>
        <%= autocomplete_field_tag :institution, params[:institution], autocomplete_institution_nome_institutions_path, :id_element => '#campu_institution_id',class: "form-control" %>
      </div>
  </div>
  </div>
  <br>
  <div class="field">
    <%= f.hidden_field :institution_id %>
  </div>
  <div class="actions">
    <center><%= f.submit 'Atualizar', class: "btn btn-primary btn-login btn-sm" %>
    </center>
  </div>

  <% end %>
 <% elsif current_user.type == "Principal" %>
 <%= form_for(@campu) do |f| %>
  <% if @campu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campu.errors.count, "error") %> prohibited this campu from being saved:</h2>

      <ul>
      <% @campu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= f.label :nome %><br>
      <%= f.text_field :name %>
    </div>
  <div class="field">
    <%= f.hidden_field :institution_id, :value => @institution.id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

 <% end %>
<% end %>

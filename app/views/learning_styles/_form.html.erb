<%= form_for(@learning_style) do |f| %>
  <% if @learning_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@learning_style.errors.count, "error") %> prohibited this learning_style from being saved:</h2>

      <ul>
      <% @learning_style.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_area :descricao %>
  </div>
  <div class="field">
    <%= f.label :sigla %><br>
    <%= f.text_field :sigla %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

$('#_users_id').empty();
$('#_users_id').html("<%= escape_javascript options_from_collection_for_select(@users, :id, :nome) %>");
$('#_users_id').prepend("<option value='todos'>Todos</option>");
$('#_users_id option:first').attr("selected", "selected");